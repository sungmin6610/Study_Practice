#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int num1, num2, num3;
	printf("정수를 입력하세요. ");
	scanf("%d""%d""%d", &num1 ,& num2, & num3);

	if (num1 >= num2)
	 if (num1 >= num3);
	 printf("%d", num1);

	if (num2 >= num1)
	 if (num2 >= num3);
	 printf("%d", num2);

	if (num3 >= num1)
	 if (num3 >= num2);
	 printf("%d", num3);
	
	return 0;
}